[project]
name = "buckets-for-zerodha"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "kiteconnect>=5.0.1",
    "python-dotenv>=1.2.1",
]
